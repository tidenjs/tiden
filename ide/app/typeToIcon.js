export default function typeToIcon(type) {
  if (type === `stream`) {
    return `⛲`
  }
  if (type === `nano`) {
    return `⚙️`
  }
  if (type === `page`) {
    return `📄`
  }
  if (type === `component`) {
    return `🟦`
  }
}
